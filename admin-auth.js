const cfg=window.MSB_CONFIG||{};const client=window.supabase?.createClient(cfg.url,cfg.anonKey);const form=document.querySelector('#loginForm'),msg=document.querySelector('#loginStatus');form?.addEventListener('submit',async e=>{e.preventDefault();msg.textContent='Signing in…';const d=Object.fromEntries(new FormData(form));const {error}=await client.auth.signInWithPassword({email:d.email,password:d.password});if(error){msg.textContent=error.message;return}window.location.href='admin.html';});