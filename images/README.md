# Ms. Lil Baker image assets

Put customer-facing cake images in this folder using stable filenames, for example:
- chocolate-hazelnut-cake.jpg
- berry-cheesecake.jpg

The website image system uses normal `/images/...` paths first and keeps the existing embedded photos only as a temporary fallback. This avoids making the page depend on large Base64 JavaScript payloads.
